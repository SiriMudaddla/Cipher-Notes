# Local database (contains encrypted user data - never commit this)
secure_notes.db
secure_notes.db-wal
secure_notes.db-shm

# Auto-generated local dev secret (only created if APP_SECRET_KEY isn't set)
.app_secret

# Environment variables / secrets
.env
.env.*

# Python
__pycache__/
*.pyc

# Streamlit local secrets file (if you ever add one)
.streamlit/secrets.toml
secure_notes_data/
